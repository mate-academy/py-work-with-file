with open(data_file_name, 'r') as data_file:
        reader = csv.reader(data_file)
        for row in reader:
            if not row:
                continue
            operation, amount = row
            amount = int(amount)
            if operation == 'supply':
                total_supply += amount
            elif operation == 'buy':
                total_buy += amount
